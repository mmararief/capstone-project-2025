const express = require('express');
const router = express.Router();

// Contoh endpoint placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Place routes placeholder' });
});

module.exports = router; 